# ==============================================================================
# Overleaf-Zotero Manager Configuration
# ==============================================================================
# Copy this file to .env and fill in your values
# cp .env.example .env

# ==============================================================================
# Flask Application
# ==============================================================================
# Generate a secure secret key with: python3 -c "import secrets; print(secrets.token_hex(32))"
FLASK_SECRET_KEY=your_secure_random_secret_key_here

# Flask server settings
FLASK_DEBUG=False
FLASK_HOST=0.0.0.0
FLASK_PORT=5000

# ==============================================================================
# Lab Branding
# ==============================================================================
# This name will appear throughout the web interface
LAB_NAME=Your Lab Name

# ==============================================================================
# Admin Credentials
# ==============================================================================
# Credentials for logging into the dashboard
ADMIN_USERNAME=admin
ADMIN_PASSWORD=changeme_to_secure_password

# ==============================================================================
# MongoDB Connection
# ==============================================================================
# MongoDB URI for Overleaf database
# If running via Docker Compose with Overleaf: use mongo:27017
# If running locally for testing: use 127.0.0.1:27017
MONGODB_URI=mongodb://mongo:27017/sharelatex?directConnection=true

# ==============================================================================
# Paths
# ==============================================================================
# Absolute paths to Overleaf Toolkit and Zotero Proxies directories
# These paths are set by scripts/configure.sh during installation
OVERLEAF_TOOLKIT_PATH=/path/to/overleaf-toolkit
ZOTERO_PROXIES_PATH=/path/to/zotero-proxies

# Docker image name for Zotero proxy containers
ZOTERO_PROXY_IMAGE=zotero-overleaf-proxy:local

# ==============================================================================
# Public URLs
# ==============================================================================
# Public URL for Overleaf (used in user-facing instructions)
OVERLEAF_URL=https://overleaf.your-domain.com

# ==============================================================================
# Signup Subdomain (Optional)
# ==============================================================================
# If using a separate subdomain for public Zotero signup (e.g., zotero-signup.domain.com),
# set this to the subdomain prefix. Visitors to this hostname will be automatically
# redirected to /zotero/signup. Leave empty to disable.
# Example: SIGNUP_SUBDOMAIN=zotero-signup
SIGNUP_SUBDOMAIN=

# ==============================================================================
# Session Security
# ==============================================================================
# Set to True if using HTTPS (recommended for production)
SESSION_COOKIE_SECURE=False
SESSION_COOKIE_HTTPONLY=True
SESSION_COOKIE_SAMESITE=Lax

# ==============================================================================
# Logging
# ==============================================================================
LOG_LEVEL=INFO
LOG_FILE=logs/app.log
